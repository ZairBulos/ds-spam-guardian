{
  "name": "ds-spam-guardian",
  "description": "Discord bot to delete spam messages",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "docker:prod": "docker-compose -f docker-compose.yml down && docker-compose -f docker-compose.yml up --build",
    "docker:dev": "docker-compose -f docker-compose.dev.yml down && docker-compose -f docker-compose.dev.yml up --build",
    "format": "npx prettier . --write",
    "test": "jest --runInBand"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "_moduleAliases": {
    "@": "src"
  },
  "dependencies": {
    "discord.js": "^14.18.0",
    "dotenv": "^16.5.0",
    "module-alias": "^2.2.3",
    "winston": "^3.17.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "prettier": "3.5.3"
  }
}
